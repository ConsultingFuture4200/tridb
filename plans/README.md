# Implementation Plans

Generated by the `improve` skill on 2026-06-24, against commit `cb097db`. Each executor:
read the plan fully before starting, honor its STOP conditions, run every verification
command, and update your row below when done.

These plans came from a deep read-only audit of the first-party TriDB code (C extension,
Python tooling, Bash build scripts, SQL tests, docs). They do **not** touch vendored
MSVBASE source except through the build scripts' patch layer.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | One-command engine verification + CI | P1 | M | — | TODO |
| 002 | Pin the MSVBASE commit | P1 | S | — | DONE (advisor/002-pin-msvbase-commit) |
| 003 | Verify all MSVBASE patches applied | P1 | S | — | DONE (advisor/003-verify-patches) |
| 004 | Regenerate gx10build.sh from the proven recipe | P1 | M | 002, 003 | DONE (advisor/004-regenerate-gx10build) |
| 005 | Fix the scalar l2_distance fork bug | P2 | M | 003 | DONE (advisor/005-fix-l2distance) |
| 006 | Docs consistency pass | P2 | S | — | DONE (advisor/006-docs-consistency) |
| 007 | Integrity-check build downloads | P2 | M | — | DONE (advisor/007-download-integrity) |
| 008 | Python tooling hardening | P3 | S | — | DONE (advisor/008-python-hardening) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 floats to the top**: it makes one command (and CI) verify the engine, so every
  later plan has a real pass/fail gate. Do it first.
- **004 depends on 002 + 003**: regenerating gx10build.sh should reuse the commit pin (002)
  and the patch-verification (003), ideally via a shared `scripts/lib/msvbase_patches.sh`.
- **005 depends on 003**: the l2_distance fix is applied as a new entry in the patch layer,
  which 003 makes verifiable.

## Findings considered and rejected (do not re-audit)

- **graph_store.c SPI error path "skips SPI_finish"**: not a leak. PostgreSQL's
  `AtEOXact_SPI` cleans up the SPI stack on the transaction abort that `elog(ERROR)`
  triggers. By-design platform behavior.
- **Dev-only credentials in baseline/docker-compose.yml** (neo4j/testpassword, postgres,
  minioadmin): intentional local test fixtures, explicitly commented "Dev-only … override
  for anything non-local", no real secret value. An `.env.example` is a nice-to-have only;
  not worth a standalone plan.
- **add_edge unbounded array growth / global visit counter / FR-7-is-principle-only /
  emission-level early termination / FROM-clause SRF materialization / scalar-distance-is-0**:
  all already documented as by-design v0 limitations in `docs/graph_store_v0_limitations.md`
  and `docs/fork_findings.md`. Settled, not findings. (Note: 005 *fixes* the scalar-distance
  bug, which the docs frame as a candidate fork patch — that's the action, not a re-report.)
- **C-extension boundary tests (empty adjacency, non-existent vertex, negative paths)**: the
  code is already correct (palloc0); these are nice characterization tests. Folded into 001's
  test expansion if desired; not a standalone plan.
