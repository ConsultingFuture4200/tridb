# Floors only. Reproducible installs: pip install -r requirements.lock (regenerate with make lock)
numpy>=2.5
pytest>=9
ruff~=0.15.18
# Baseline harness clients (DEV-1171)
neo4j>=6
pymilvus>=2.4,<2.7  # caps below a 2.7 major; the 2.6-client/2.4.5-server gap is a pending decision (baseline/TUNING.md)
psycopg[binary]>=3.1
# GraphRAG QA benchmark embedder (Plan 015) — BGE-768 via onnxruntime, no torch
fastembed>=0.8
# Phase-0 wiki extraction (docs/wiki_scale_benchmark_spec) — wikitext -> plain text + links
mwparserfromhell>=0.7
# Recall-decay-under-updates bench — the engine's own HNSW lib (host-side)
hnswlib>=0.8
# Public-ANN dataset reader (ann-benchmarks .hdf5) for the one-command repro
# (make bench-repro). Soft dep elsewhere; required only to build the SIFT oracle.
h5py>=3.10
# Wikidata full-dump fast path (tools/wikidata_compact.py + ingest --compact) — fast
# per-line json parse; optional at runtime (stdlib json fallback, tests never require it)
orjson>=3.9
