# TriDB Benchmark — GraphRAG QA Accuracy (HotpotQA dev slice, Plan 015)

**Real graph, real embeddings, real multi-hop questions.** Injecting REAL graph bridges into the retrieved context lifts multi-hop **joint** evidence recall@5 (both supporting paragraphs found) on `bridge` questions by **+15.6%** over vector-only (72.1% -> 87.7%), on 150 HotpotQA dev questions / 1490-paragraph corpus.

- Graph = REAL title-mention edges (embedding-INDEPENDENT proxy for Wikipedia hyperlinks): rebuilding with a different encoder does not move an edge — so this is a true test that *topology* (not the vectors) carries the multi-hop signal.
- Embeddings = BGE-base-en-v1.5 (dim 768), cosine.
- **The mechanism matters (honest negative + positive):** `graph_inject` adds graph-reachable bridges to the context regardless of query similarity. `graph_rerank` (vector-seed -> reachable -> re-rank by query cosine) is the naive version and does NOT help — it still buries the hard 2nd hop and restricts the pool at loose k. Both are shown.

## Evidence recall vs k — joint (BOTH gold) and any-gold (no LLM)

Group: `bridge` questions (the multi-hop case the graph targets).

| k | vector_only joint | graph_inject joint | graph_rerank joint | inject lift |
|---:|---:|---:|---:|---:|
| 2 | 0.443 | 0.443 | 0.443 | +0.000 |
| 3 | 0.607 | 0.779 | 0.607 | +0.172 |
| 5 | 0.721 | 0.877 | 0.721 | +0.156 |
| 10 | 0.918 | 0.959 | 0.893 | +0.041 |

Any-gold recall@k (same group):

| k | vector_only | graph_inject | graph_rerank | inject lift |
|---:|---:|---:|---:|---:|
| 2 | 0.705 | 0.705 | 0.705 | +0.000 |
| 3 | 0.799 | 0.885 | 0.799 | +0.086 |
| 5 | 0.857 | 0.934 | 0.857 | +0.078 |
| 10 | 0.959 | 0.980 | 0.943 | +0.020 |

### By question type (joint recall @ k=5)

| type | vector | graph_inject | lift |
|---|---:|---:|---:|
| bridge | 0.721 | 0.877 | +0.156 |
| comparison | 1.000 | 1.000 | +0.000 |

## Downstream answer accuracy (EM / F1) @ k=5

Reader = codex-cli (operator subscription); 40 questions.

| retriever | answer EM | answer F1 | ev recall | ev joint |
|---|---:|---:|---:|---:|
| vector_only | 0.600 | 0.744 | 0.887 | 0.800 |
| graph_inject | 0.625 | 0.769 | 0.950 | 0.925 |

## Honesty notes

- **Where graph helps:** at TIGHT, realistic RAG budgets (k=3-5) on multi-hop (`bridge`) questions — exactly where vector-only misses the low-query-similarity 2nd hop. At loose k the pool saturates and the lift shrinks; on single-hop (`comparison`) questions there is no 2nd hop to recover, so graph ~ vector. This k/type dependence is the real finding, reported as a curve, not a point.
- **Scope:** host-side accuracy on a 150-question dev slice / 1490-paragraph corpus (HotpotQA `distractor` pool, gold guaranteed present). The full retrieve-from-all-Wikipedia run and the live tjs() latency-at-fixed-accuracy headline are GX10/engine-gated (scripts/bench_graphrag.sh).
- **Graph is a mention-proxy, stated plainly:** the official Wikipedia hyperlink dump was unreachable here (CMU host down; HF mirrors gated). Title-mention edges are a faithful, embedding-independent stand-in; the on-target run can swap in the real hyperlink dump (the manifest records the edge source).

## Reproduce

```bash
make fetch-hotpot HOTPOT_Q=150     # HotpotQA dev slice (HF mirror; CMU host down)
make graphrag                      # real graph + BGE-768 + accuracy sweep
#   GRAPHRAG_READER=anthropic make graphrag   # LLM EM/F1 headline (needs API key)
```

_Generated by `bench/graphrag_report.py` (`make graphrag`). Numbers are observed._
