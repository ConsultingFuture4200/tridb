# Implementation Plans (advisor)

Generated by the `improve` skill on 2026-06-26 (deep audit, Linus-grade), planned against commit `7bf3dca`.
Written to `advisor-plans/` because the repo's `plans/` already holds the project's own phase plans.

These are handoff plans for an executor with **zero context**. Read the plan fully, honor its STOP
conditions, update your row when done. The repo builds in two layers: the **Python layer**
(`make test` = pytest, `make lint` = ruff) runs anywhere; the **engine layer** (the MSVBASE fork +
native graph AM) builds only via Docker and the GX10 — plans touching `vendor/MSVBASE` or `src/graph_store`
C are **GX10-gated** (author + review here, build/verify on the GX10).

## 2026-07-09 batch — post-DEV-1354 deep audit (planned against `c216750`)

Deep `/improve` audit (5 parallel category auditors + advisor re-vet) after wiki fusion speed (200k),
consistency demo, batched `gph_insert_edges`, and wiki-reader landed. Prior plans 001–039 treated as
landed; these 20 plans are **residuals only**. All findings re-opened against live code at `c216750`.

| Plan | Title | Priority | Effort | Risk | Depends on | Verify here? | Status |
|------|-------|----------|--------|------|------------|--------------|--------|
| 041 | Fix README SM-1 headline to honest 1.07× FAIL | P1 | S | LOW | — | YES | **DONE** (`advisor/041-…` `8752349`) — SM-1→1.07× FAIL; advisor spot-verified |
| 053 | Multicol fork harness: labeled marker, not bare `2000` | P2 | S | LOW | — | partial (engine) | **PENDING-GX10** (`f094dea`) — labeled tag marker + fixture, static-clean |
| 054 | `make clean` must not wipe `data/` | P2 | S | LOW | — | YES | **DONE** (`103fabb`) — `clean` preserves `data/`; new `clean-data` needs `CONFIRM=1`; spot-verified |
| 051 | wiki-reader: token + body cap on mutating POSTs | P1 | S | LOW | — | YES | **DONE** (`4f6a023`) — token + body cap + 60-line tests; spot-verified |
| 045 | Type-filter `gph_tombstone_edge` (typed edges) | P1 | S | LOW | — | engine | **PENDING-GX10** (`da9b807`) — authored + static-clean |
| 046 | `gph_insert_edges` rejects tombstoned dst (parity) | P1 | S–M | LOW–MED | — | engine | **REVISE** (`0a8aae8`) — C fix correct; FIX: `graph_store_am_persist.sql` vertex_count 6→5 + re-run `graph_am_test` |
| 047 | `gph_neighbors_ext_cached` honors `identity_mode` | P1 | S–M | LOW | — | engine | **PENDING-GX10** (`c3a81e4`) — authored + static-clean (no real defects) |
| 040 | Freeze **xmax** so tombstones survive clog truncation | P1 | M | MED | — | engine | **PENDING-GX10** (`7cc3b41`) — xmax freeze authored; add vertex-tombstone test on GX10 |
| 042 | Wiki extract shard rotate: never truncate on revisit | P1 | M | MED | — | YES | **DONE** (`98c392d`) — never-truncate on rotate + test; spot-verified |
| 044 | wiki_engine_serve: no published trust-auth Postgres | P1 | M | MED | — | bash -n + manual | **DONE** (`8f19ede`) — loopback-default bind + per-run scram password; `pg_hba 0.0.0.0/0` documented residual |
| 050 | Host unit tests for DEV-1354 harness gates | P1 | M | LOW | — | YES | **DONE** (`f29b0cf`) — 16 tests, `publication_gate` matrix + `torn`/`vec` |
| 048 | Dense O(1) locate on traversal `gs_open` | P1 | M | MED | — | engine | **PENDING-GX10** (`73d8e29`) — default-off GUC, zero behavior change |
| 049 | Hold `Relation` across neighbor `Next()` | P1 | M | MED | 048 optional | engine | **REVISE** (`7394b2d`) — FIX: cleanup path for early SRF abandonment (relcache-leak on undrained scan) |
| 057 | `tjs_open` expand via `gph_neighbors_ext_cached` | P2 | M | MED | **047** | patch CI + engine | TODO (deferred — do after 047 lands) |
| 055 | Honest edge-count semantics after deletes | P2 | M | MED | — | engine | **PENDING-GX10** (`1a43863`) — authored + static-clean |
| 052 | HNSW `vector_index_map` invalidation (ADR-0014 A) | P2 | M–L | MED | coord. 043 | patch CI + engine | **PENDING-GX10** (`0db7d3b`) — patch authored; `ci_check_patches` green |
| 058 | Core `requirements.lock` vs optional extras | P2 | M | MED | — | YES | **DONE** (`05ba1b1`) — core lock + `requirements-vdbb.txt` extras; spot-verified |
| 059 | ADR-0013 Stage C: archive v0 `graph_store_ext` | P2 | M | MED | Stage A/B done | engine | **BLOCKED** — v0-only ENGINE_TESTS still wired (`graph_store_test.sql`, `graph_v0v1_parity_test.sql`) + ADR-0013 defers Stage C; plan needs rework before re-run |
| 043 | Unblock 1M fused vector/HNSW leg | P1 | L | HIGH | — | GX10 | TODO (excluded from fan-out — GX10 HNSW-iterator debugging, not mechanical) |
| 056 | Graph-leg snapshot isolation (DEV-1166 residual) | P2 | L | HIGH | **040** | engine + live S3 | TODO (excluded — needs 040 landed first) |

**Fan-out `wf_2c0131ff-31a` (2026-07-09):** all 16 executed in isolated worktrees, scope-clean across the board. 6 host-plans **DONE** (spot-verified), 7 engine/patch plans **PENDING-GX10** (authored + static-clean, build deferred to the GX10), 2 real **REVISE** defects (046, 049), 1 **BLOCKED** (059 needs plan rework). Branches persist as `advisor/NNN-*`; nothing merged.

**Engine-verification pass (2026-07-10, `tridb/msvbase:dev` x86 image):** built + ran `make graph-test` per plan.
- **ENGINE-VERIFIED GREEN (mergeable):** 040, 047, 048, 052 (+`ci_check_patches`), 053, 055 — and **046** after the agent fixed the stale `persist.sql` vertex_count 6→5 (commit `7a70b3c` on `advisor/046`).
- **045 — GREEN after re-diagnosis:** the "regression" was a MISDIAGNOSIS — the C type-filter (`da9b807`) is correct; the failure was a **stale vertex-numbering bug in plan 045's own test** (Test E's `add_edge` auto-created vids 6,7,8; Test F wrongly assumed 6,7 were fresh). Fixed the test (`f94a23a`, retarget to fresh vids 9,10); `make graph-test` exit 0 with F1/F2/F3 green (typed semantics confirmed). Agent correctly pushed back on the dictated diagnosis.
- **049 — RED, deferred:** confirmed real `relcache reference leak` on early-abandon paths (3 WARNINGs; needs an abort-safe cleanup + a new abort-path test — do not merge; write a dedicated plan).

**Merged into `dustin/dev-1354` (2026-07-10):** all **8 GREEN engine plans** — 053, 052, 040, 046, 047, 048, 055, **045** — merged (055 + 045 needed graph_am.c conflict resolution, both clean; `_PG_init`+`gph_visible_edge_count` and the tombstone docstring reconciled). Combined-tree `make graph-test` re-verified green. Host merges earlier: 050, 042, 041, 054, 058, 051. **044 now MERGED too** (2026-07-10, `5fc8b0e`) after the other agent's `bench/wiki_h2h_vecrun.py` WIP was committed + reconciled — secures `wiki_engine_serve.sh` (loopback-default bind + scram). Still unmerged: **049** (needs the abort-path fix — now specced as **plan 061**, do NOT merge 049 without it), **059** (BLOCKED, needs rework). Branch pushed to `origin/dustin/dev-1354` (`5fc8b0e`); not merged to master.

**Plan 061 (2026-07-10, `e550e05`):** abort-safe Relation cleanup for the graph traversal SRFs — fixes the relcache-reference leak plan 049 introduced (3 confirmed WARNINGs on early-abandon/LIMIT/abort paths). Engine-gated; writes the missing abort-path leak test first, then an abort-safe release (recommended: don't hold the ref across `Next()`). TODO.

### Recommended execution order

**Wave A — host-only quick wins (parallel-safe):**  
`041 → 054 → 053 → 051 → 050 → 042 → 058`

**Wave B — graph_am correctness cluster (serialize or one PR):**  
`045 → 046 → 047 → 040 → 055`  
then `048 → 049 → 057` (perf path; 057 needs 047)

**Wave C — security serve + archive:**  
`044` ‖ `059` (disjoint)

**Wave D — fork HNSW (serialize patches):**  
`052` then/or with care `043` (both touch HNSW; do not weaken publication gates)

**Wave E — large correctness:**  
`056` only after `040` green

### Dependency notes
- **047 → 057**: identity-mode parity must hold before `tjs_open` switches to cached expand.
- **040 → 056**: freeze xmax before snapshot SI reworks visibility (else clog + SI compose badly).
- **043** is the publication gate for any 1M fusion claim; do not fabricate latency if blocked.
- **048/049** are independent of 043; they help expand cost after the vector leg works.

### Findings considered and rejected this batch (do not re-plan)
- Concurrent multi-writer adjacency races — explicit single-writer contract in `graph_am.c` header.
- Batch WAL multi-record as partial-visible bug — uncommitted xmin filter is sound.
- Baseline `testpassword` / compose tags — local fixtures; digest pin residual only.
- Re-running PG17 spike — ADR-0015 complete; decision hygiene only if desired later.
- GPU batched top-k / GraphBLAS frontier — TR-1 violations (prior reject list).
- Plans 001–039 items as open bugs — assumed landed unless residual evidence above.

### Direction (not separate plan files; covered by 043/056 + existing ADRs)
- D2 PPR+FR+RRF into engine — still ADR-0012 next iteration (not re-planned; host ref done in 007).
- D4 gBrain reverse adjacency — ADR-0016; D5 PERF-08 last mile — gpu_index_build design.
- D6 streaming graph predicate / SM-1 redesign — called out in 041 maintenance; full L effort later.

### Direction plan added 2026-07-09 (planned against `1f0628f`)

| Plan | Title | Priority | Effort | Risk | Depends on | Verify here? | Status |
|------|-------|----------|--------|------|------------|--------------|--------|
| 060 | Wikidata-on-TriDB spike — prove the differentiator at ~16× wiki scale | P1 | L | MED | 038 (hard); 043 (vector-first mode only) | ingest+harness lint/host; 10M run GX10 | **HOST-DONE** (`71bdcfa`..`1a94eee`) — ADR-0018 + `tools/wikidata_ingest.py` + Harness A `bench/wikidata_consistency.py` + Harness B `bench/wikidata_h2h.py` + 32 host tests + `docs/wikidata_spike_v0.1.0.md`; `make test` 314 / `make lint` green. Verdict INCONCLUSIVE (GO-pending one GX10 measurement pass: ingest a pinned truthy slice, Harness A `--live`, Harness B `report` through the reused gate) |

Rationale: Wikidata (~110M entities / ~1.5B **typed** statements) is the natively-tri-modal, mutation-heavy
public corpus that lets TriDB prove its **demonstrated** value (one-WAL cross-modal consistency + fused
early-terminating retrieval, ADR-0017) at a scale a stranger can reproduce, against the exact
Milvus+Neo4j+Postgres stack people run today. Primary experiments use the **filter-first** path (green at
1M, DEV-1290), so they do NOT depend on the blocked seedless-fusion leg (043). This is a DESIGN+SPIKE
plan alongside 043/056 — not part of the autonomous execute fan-out.

---

## 2026-07-03 batch — post-DEV-1290 deep audit + persona review + landscape research (planned against `e345998`)

Sources: 7-category deep `/improve` audit + Fabio/Linus/Liotta persona reviews + 4-angle landscape
research (`docs/landscape_review_v0.1.0.md`, appendix has all findings/sources). All findings vetted
against the code by the advisor before planning. Execution authorized to run autonomously with
per-plan persona verification (Fabio/Linus/Liotta) — plans that hit STOP conditions report BLOCKED,
never improvise.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 024 | Operator arg hardening + memory lifecycle (crash/leak class + lowering regression) | P1 | M | MED | — | **MERGED** (`0d3a2d4`, persona 3/3) |
| 025 | ADR-0013 Stage A+B: operators+benches onto the v1 native AM + docs truth pass | P1 | L | MED-HIGH | 024 | **MERGED** (`18696cb`, persona 3/3; 1M v1 recall 1.0 / 13.4×) |
| 026 | Graph-store safety riders: ACLs, VACUUM/wraparound docs, freeze design note | P1 | S | LOW | — | **MERGED** (`64116cc`, persona 3/3) |
| 027 | CI nightly engine gate + KEEP_GOING + 4 missing high-value tests | P1 | M | LOW | — | **MERGED** (`bcf3610`, persona 3/3) |
| 028 | PG17 platform feasibility spike + public "why a fork" ADR | P1 | M | LOW | — | **MERGED** (`9a855aa`, persona 3/3; ADR-0015: zero PG17 API drift, only 32KB-page bind) |
| 029 | Hot-path perf: SIMD drain distance, hash-join membership (O(D) loads moot post-025) | P2 | M | MED | 024/025 merged | **DEFERRED** — needs engine cycle; share with a future rebuild (see plan addendum) |
| 030 | Benchmark credibility: p95/p99, HNSW row, dep hygiene (steps 2/4 deferred) | P2 | M-L | LOW-MED | — | **MERGED** (`7031c8d`, persona 3/3; steps 2 multi-client + 4 dataset-pin deferred) |
| 031 | FR-6 calibrated cost decision core (wire-up + boundary sweep deferred) | P2 | M | MED | 025 | **MERGED** (`7954622`, persona 3/3; core only, default-off, wire-up gated on deg(src) accessor + sweep) |

Recommended order: **024 → (026, 027, 028 in parallel) → 025 → (029, 030, 031)**. Engine-image
builds and `make graph-test` runs are serialized (one docker-heavy plan at a time); 026/027/028
are parallel-safe (disjoint files; 026 coordinates with 025 on `graph_store_am--0.1.0.sql`).
Publication freeze on external benchmark numbers until 025 lands (F1).

---

## 2026-07-04 batch — performance research quick-wins (planned against `876a696`)

The x86-authorable quick-win tier of the performance audit (`docs/perf_research_v0.1.0.md`, PERF-01..12,
four code-grounded researchers). These four need **no** GX10 build to author and mostly verify on the
standin — PERF-01's patch applies + builds x86 (NEON path inert), the rest are SQL/loader/tooling. Each
has a Linear issue. The heavier GX10 build-cycle batch (PERF-04/05/06/07) and structural bets
(PERF-08/09/10/12) are tracked in the perf doc but NOT planned here.

| Plan | Title | Priority | Effort | Risk | Depends on | Linear | Status |
|------|-------|----------|--------|------|------------|--------|--------|
| 032 | NEON inner-product/cosine kernel (default metric runs scalar on ARM) | P1 | S | LOW | — (sibling DEV-1288) | DEV-1343 | **IMPLEMENTED** — branch `advisor/032-neon-ip-kernel`; patch `tridb_neon_ip_distance.patch` registered + sentinel, applies+reverses clean vs live vendored hnsw; `neon_ip_bench.c` compiles+correct on x86; pending GX10/engine build (NEON compile, per-call speedup, IP-index build-time drop) |
| 033 | Dense-id identity fast-path for the v1 graph id-map (~2ms) | P2 | S–M | LOW | 025 | DEV-1344 | **IMPLEMENTED** — branch `advisor/033-dense-id-fastpath`; x86 pytest+ruff green; parity oracle + graph-leg ms pending GX10/engine build |
| 034 | Backend-local cached vid map (general sparse-id ~2ms) | P2 | M | LOW–MED | 025 | DEV-1345 | **IMPLEMENTED** — branch `advisor/034-vid-cache`; pending GX10/engine build |
| 035 | COPY bulk load — unblock 128GB saturation + fair at-scale SM-2 | P2 | M | LOW | 025 | DEV-1346 | **IMPLEMENTED** — branch `advisor/035-copy-bulk-load`; x86 pytest+ruff green + emitted-SQL inspected; parity + load-time ms pending GX10/engine build |

Recommended order: **032 (highest leverage — latent cosine-workload bug) → 035 (unblocks at-scale
measurement) → 033 → 034**. 033 and 034 are complementary (dense vs sparse id loads); ship both.
Engine-image rebuilds stay serialized; 035 and the 033/034 SQL work touch disjoint files.

---

## 2026-07-04 batch — gBrain backend hardening, critical path (planned against `cb3eb0a`)

The additive engine hardening that makes TriDB a viable backend for **gBrain** (AgentBOX memory, now on
the Spark) — spec `docs/gbrain_backend_hardening_v0.1.0.md`. All three are **native graph-store C**
(GX10-gated build): author + persona-review + static-verify here, engine build + FR-7 re-run on the GX10.
All are **additive** — no frozen-core edit, no golden-rule break (TR-1, native AM, one-WAL/FR-7, one
canonical surface, three stores preserved). Cosine (B4) decided: **normalize-at-write first** (order-
identical, zero engine change) with true-IP riding PERF-01 as a follow-on.

| Plan | Title | Priority | Effort | Risk | Depends on | Linear | Status |
|------|-------|----------|--------|------|------------|--------|--------|
| 036 | `gph_freeze()` + disarm forced anti-wraparound autovacuum (long-lived-store gate) | P1 | M | MED | 026 (design) | DEV-1347 | **IMPLEMENTED-pending-GX10** — branch `advisor/036-gph-freeze`; `gm_reserved`→`gm_frozen_horizon` (size-assert, no layout change), `gph_freeze(horizon xid)` GenericXLog page-walk (committed→Frozen / aborted→Invalid, relfrozenxid advance, idempotent) + `test/graph_freeze_test.sql` wired into AM_TESTS; x86 pytest+ruff green; C unbuilt here (needs PG 13.4 image); STOP #3 hit — forced-autovacuum disarm is INDIRECT (keep age low) + auto-freeze table-AM stage deferred |
| 037 | native graph delete via `gph_tombstone_edge/vertex` | P1 | S–M | LOW | — | DEV-1349 | **IMPLEMENTED-pending-GX10** — branch `advisor/037-graph-tombstone`; x86 pytest+ruff green; C authored-but-UNBUILT (needs the fork image). DEVIATION from the plan's bare-flag model (STOP hit): a bare `GPH_FLAG_DELETED` is NOT abort-atomic (GenericXLog has no in-process UNDO), so delete-then-rollback would wrongly persist — FR-7 fail. Fixed by stamping the deleting xid in the repurposed `es_xmax`/`vr_xmax` (size-stable, static-assert unchanged) + a one-line read-path visibility check (`gph_deleted_visible`), byte-identical for all pre-037 records. `graph_delete_test.sql` + runner added; FR-7 rollback/idempotency/remove_edge covered; committed- and uncommitted-tombstone crash-recovery cases added to `crash_recovery_test.sh`/`crash_recovery_assert.sql` (WAL-redo of a committed tombstone; a crash-aborted tombstone reads live via xid-visibility); GX10 `make graph-test` + crash-recovery RUN pending |
| 038 | typed + directional + source-scoped native traversal | P1 | M | MED | — | DEV-1350 | **IMPLEMENTED-pending-GX10** (typed insert+dict, typed/source-scoped out-traversal, `gph_traverse_typed`; direction=in/both deferred to ADR-0016 reverse index; C authored-unbuilt) |

Recommended order: **036 (correctness gate — nothing long-lived is safe without it) → 037 → 038**. 037
and 038 touch the same `graph_am.c` traversal region, so serialize or merge carefully. Deferred (tracked,
not planned here): A2 HNSW abort-durability (DEV-1348), B3 edge-property side-table, B4 true-IP cosine
(B4-interim = normalize, no plan needed), B5 multi-dim vectors, C1 the `BrainEngine` adapter (cross-repo).

---

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Verify here? | Status |
|------|-------|----------|--------|------|------------|--------------|--------|
| 001 | Harden + test the benchmark grading layer | P1 | M | LOW | — | YES (`make test`) | DONE (`f604c27`) |
| 002 | CI: verify the fork patch-chain applies | P1 | M | MED | — | partial (CI is the gate) | DONE (`74c59ef`) — full CI behavior verifies on next PR run |
| 005 | Fix stale @-scale parity row in the GTM doc | P2 | S | LOW | — | YES (read-back) | DONE (`f6033dd`) |
| 003 | Thread HNSW reloptions into crash-recovery rebuild | P2 | S | LOW | — | NO (GX10-gated) | DONE (`7a198af`) — patch git-apply-verified; GX10 build/run + a crash-driver shell still pending |
| 004 | One-command sweep repro: `scripts/bench_gx10_sweep.sh` + `make sweep` | P2 | M | LOW | — | partial (script lints here; runs on GX10) | DONE — `bash -n`/`make -n` verified; live run GX10-gated |

Recommended order: **001 → 002 → 005** (all cleanly verifiable on an x86 box, highest leverage),
then **003 → 004** (GX10-gated execution/verification).

---

## Research-driven "steal ideas" plans (added 2026-06-28, planned against `8b19cb5`)

Generated by the `improve` skill's `deep` research variant: a 6-agent workflow mined open-source DB
internals + bleeding-edge research (CIDR/SIGMOD/VLDB/PVLDB 2023-2026) for borrowable ideas, vetted
against TriDB's locked invariants (TR-1, one-WAL, native graph AM, one canonical query), and ranked by
leverage-for-TriDB. 61 raw findings → 14 ranked → these 4 spike/design plans for the clusters the
maintainer selected. Findings #1 and #6 were code-verified against `src/graph_store/` before planning.

| Plan | Title | Cluster (audit #) | Priority | Effort | Risk | Verify here? | Status |
|------|-------|-------------------|----------|--------|------|--------------|--------|
| 006 | Graph metapage edge-count/degree stats → make FR-6 bind | #1 | P1 | S | MED | engine suite on x86 image | **DONE — engine-validated** (ARM sign-off optional) |
| 007 | `tjs_open` ranking+termination host reference (PPR + NRA/FR + RRF) | #2/#3/#9 | P1 | M | LOW | full host run | **DONE — real HotpotQA, beats oracle** |
| 008 | Idle-GPU: offline CAGRA/cuVS index build (default-OFF) + RaBitQ quant | #4/#5 | P2 | M | MED | RaBitQ on real SIFT; CAGRA build+export+recall on GB10 | **DONE (host) · CAGRA VALIDATED on GB10** (cuVS 26.06; recall A/B measured GPU-side + latent hierarchy bug fixed; fork-load remaining) |
| 009 | Sorted-by-dst / CSR-lite adjacency layout spike | #6 | P2 | L | HIGH | contiguity prototype + residency bench on x86 image | **DESIGN-DONE · contiguity prototype built → leaning GO** (FR-7 safe; ~830× fewer page reads; disk-seek number GX10-gated) |

Status legend: **DONE — engine-validated** = built + full engine suite passed on the `tridb/msvbase:dev`
x86 image; **DONE (host)** = host-runnable scope verified on this box; **DESIGN-DONE** = design deliverable
complete; **BLOCKED** = a hard external dependency (cuVS not installed on the GX10); **deferred** = a
HIGH-risk change left for deliberate (not autonomous) execution. Real measured results, commit SHAs, and
branch names are in the "Execution + review outcome" section below.

Recommended order: **006 → 007** first (highest leverage; 007 is fully runnable here and de-risks the
committed next build), then **008** (the 128 GB / GPU launch lever), then **009** (biggest structural
bet, gate for compression + WCOJ — do it last and only if 006's degree stats / traversal numbers say
the layout is the bottleneck).

### Dependency notes (research plans)
- **006 ⟶ 007**: 006's `gm_edge_count`/degree stats feed 007's PPR seed-weighting (node specificity);
  independent but complementary — 007 falls back to uniform weights if 006 hasn't landed.
- **009 gates** adjacency compression (audit #14) and WCOJ — neither should start before 009's go/no-go.
- **008 Step-1 (RaBitQ recall sim)** is a prerequisite for any in-engine quantization follow-on and for
  feeding 007's termination bound; do it before committing GX10 quantization C.

### Ranked findings NOT turned into plans (this run) — pick up later, don't re-audit

Surfaced + vetted, leverage real but below the selected clusters or a deliberate follow-on:
- **#7 ACORN 2-hop expansion + NaviX in-loop selectivity switch** (SIGMOD'24 / VLDB'25) — predicate-aware
  iterator expansion for the selective-predicate failure mode; pairs with 007. Medium leverage; defer.
- **#8 pgvector discarded-candidate heap + dual scan budget** (pgvector 0.8, iss #678/#862) — concrete
  data structure for `tjs_open` frontier resumption + a `term_cond` backstop; folds into 007's (B) build.
  Note the #862 landmine: a relaxed-order index must NOT advertise strict-sort pathkeys (heed when
  ADR-0007 migrates `tjs()` to a CustomScan).
- **#10 cheap query-side router for FR-6** (EA-GraphRAG 2026) — entity/hop-count features pick
  vector-only vs graph-expand, no cost model; complements 006 (data side) with the query side. Low effort.
- **#11 SBQ binary quant as a NEON popcount(XOR) kernel** (pgvectorscale SBQ) — the simplest NEON
  quantization kernel; overlaps 008's RaBitQ (RaBitQ gives the better bound, SBQ the simpler kernel —
  prototype both in 008, pick one).
- **#12 eddies-lite adaptive predicate order** (Eddies SIGMOD'00; J*/FRPA) — learn-then-commit predicate
  evaluation order inside `tjs`; the streaming-granularity version of the deferred v2 adaptive replanning.
- **#13 MuSiQue/2Wiki recall@k as the `tjs_open` report bar** (HippoRAG2 2025) — MuSiQue (~50 recall) is
  the discriminating open-domain metric; report a curve. Pure harness/reporting; sequence with DEV-1173.
- **#14 delta+VByte adjacency compression** (Aspen PLDI'19) — 4-8× more neighbors/page at scale;
  **strictly downstream of plan 009** (needs sorted dst + a parallel version array). v3+.

### Considered and REJECTED (vetted against TR-1 / one-WAL / scope — do not re-propose)
- **GPU batched radix top-k** (RadiK/AIR-Top-K) for the final k-selection — materialize-all + blocking,
  **violates TR-1**. GPU's defensible use is the offline build (plan 008) and the per-candidate distance
  kernel, never the rank merge.
- **GraphBLAS masked SpMV / Gunrock bulk-synchronous frontier** for multi-hop expansion — level-synchronous,
  materializes the whole frontier, **violates TR-1** on the emission path. (The membership mask *is*
  conceptually a GraphBLAS mask — but as documentation, not an executor.)
- **Sirius / TQP GPU columnar relational engine** (libcudf) for the relational leg — bulk/vectorized,
  blocking; **violates TR-1** as a canonical executor.
- **DuckPGQ on-the-fly CSR build** over the reachable set — blocking materialization **and** against
  golden rule 3 (TriDB's thesis is a *native persistent* adjacency AM). It's the anti-pattern that
  confirms the native-AM choice.
- **HippoRAG as published** (PPR to convergence, then sort all passages) — materialize-all + sort-before-emit,
  the exact blocking shape ADR-0012 must avoid. The compliant form (bounded forward-push PPR) is plan 007.
- **DuckDB push-based executor rewrite** — out of scope (XL rewrite of a PG-13.4 pull-based fork for no
  required gain); the salvageable streaming top-k micro-pipeline is already implicit in `tjs_open`.
- **Custom WAL Resource Manager (`RegisterCustomRmgr`)** for the graph AM — **PG15+, unavailable on the
  PG-13.4 fork**. GenericXLog is the *correct* one-WAL choice on 13.4 (gives SM-5 free), not a limitation.
  Revisit only after a rebase past PG15 *and* if logical decoding of graph edges is ever required.
- **NaviX CSR-HNSW substrate unification / full Filtered-DiskANN StitchedVamana / WCOJ leapfrog-triejoin**
  — deferred (research-only) for the single-linear-hop v1 canonical query; the borrowable kernels
  (NaviX in-loop selectivity → #7; filtered entry-point selection → #7) are taken without the heavy parts.

### Research provenance
Workflow run `wf_359a79e9-a6d` (6 domains: ANN/GPU, graph-storage, execution/rank-operators, GraphRAG,
Postgres-AM-craft, frontier-research). Full synthesis (themes + all 14 ranked + 12 rejected with reasons)
captured in the session transcript; this index carries the durable summary.

### Execution + review outcome — ROUND 2, real validation (2026-06-29)
A second execution round ran each plan to its *real* completion on this box, which turned out far more
capable than the conservative "GX10-gated" labels implied: the `tridb/msvbase:dev` **x86 engine image is
present** (and PGXS-builds `src/` from the worktree at test time), the **HotpotQA + SIFT datasets are
local**, and the **GX10 (`ssh spark`) is reachable**. Each was executed by an isolated-worktree executor
and reviewed (advisor re-ran the checks, read every diff, audited the new tests — no self-report trusted).
Verdict: **all four APPROVED with real, measured results.**

- **006 — DONE, engine-validated on the x86 image (not just authored).** The metapage C **compiled and
  passed the full native-AM engine suite** on `tridb/msvbase:dev`: `graph_am_test` PASS, `join_order_test`
  FR-6 parity PASS, `graph_edge_count_test` PASS (init 0 → branches 1+2 = 4 → multi-page chain = 1504),
  `crash_recovery` **both scenarios PASS** — including scenario 2 proving an *uncommitted* edge's increment
  is absent after a crash (`gm_edge_count = 0`), the abort-safety the plan required. Step 5 Makefile wiring
  applied and built. `make test` 152 / lint clean. Frozen `join_order.c` untouched (verified empty diff).
  **Executor caught + corrected a real bug in the *authored test*:** it had asserted a clean `ROLLBACK`
  reverts the counter (1506→1504), which is impossible — GenericXLog gives crash-recovery REDO, not
  in-process UNDO — and fixed it to the honest v1 behavior (stays 1506; the raw counter is not in-process
  abort-aware, like `gph_vertex_count`), with crash-recovery scenario 2 proving the abort-safety that *is*
  guaranteed. Two harness files were edited (in-scope) so the engine build resolves the new `gph_page.h`
  include. Branch `advisor/006-metapage-degree-stats` (`…ad05217`, `4ccc8ff`). **Remaining:** only an
  optional ARM sign-off of the identical C on the GX10 (no ARM-specific code) — not a correctness gate.
  *Nit:* the `graph_edge_count_test.sql` header comment (lines 4-5) still says "rolls back atomically on
  ABORT" and should be reconciled with the corrected body.
- **007 — DONE, real HotpotQA, and it BEATS the blocking oracle.** Ran on the real 1490-para / 745-edge /
  150-question manifest (BGE-768): **FR-fused recall@5 0.937 and RRF-fused 0.907 both exceed the (A)
  blocking oracle's 0.883**; FR recall@10 **0.987** > vector-only 0.967 — all while examining **~0.71% of
  the corpus** (the TR-1 early-termination proxy, with huge margin). The streaming TR-1-pure composition is
  not merely within tolerance of the blocking reference — it surpasses it. Honest caveats logged in the
  ADR-0012 addendum: `ppr_only` alone (0.863@5) underperforms vector — graph is a *fusion* signal, not a
  standalone retriever on this sparse graph; and `r_max` is inert here (flat curve, mean reach ≈ 10 nodes),
  so its recall/examined tradeoff only bites on a denser/at-scale graph (GX10 follow-up). This de-risks the
  committed `tjs_open` (B) build with real evidence. Branch `advisor/007-tjs-open-ranking` (`b8bf340`).
- **008 — DONE (host), real SIFT; CAGRA build+export now VALIDATED on the GB10.** RaBitQ on real
  `sift-128` (two agreeing runs): **4-bit + in-scan rerank → recall@10 = 1.0 at 7.5×** compression; 8-bit
  raw 0.99. This *corrected* the synthetic finding — **1-bit is dead on real data** (rerank doesn't rescue
  it: 0.07@R=100; the synthetic 1-bit viability was an easy-clustering artifact), so the honest
  recall-preserving lever is ~7.5× (4-bit), not 8-32×. The design note's footprint/toggle analysis stands
  (zero serving-path GPU footprint, default-OFF `WITH_CUVS`). **CAGRA UNBLOCKED (2026-06-29):** cuVS
  **26.06** was provisioned on the GB10 (aarch64 + CUDA 13 + sm_121, isolated `~/cuvs-env`), and the build
  path is **verified live** — `cagra.build` (20k×128 in ~1.96 s) + `hnsw.from_cagra(IndexParams(), idx)` →
  `hnsw.save` produces a 13.2 MB HNSW file. `scripts/gpu_build_index.py` was reconciled to the real cuVS
  26.06 API (the authored `from_cagra(index)` form was wrong). **Remaining:** the recall A/B (load the
  exported file through the fork's `hnsw` AM vs a CPU-built index) — now a staging+format-check task, not a
  provisioning blocker. Branch `advisor/008-gpu-build-quant` (`e0641f4`) + the 2026-06-29 doc/script updates.
- **009 — DESIGN-DONE; two-round spike → lean NO-GO upgraded to LEANING GO.** Round 1 (delta-tail sorted
  layout, branch `spike/009-csr-lite-prototype` `c90683c`): compiles, correct (`nondecreasing=t`), FR-7-safe
  (zero divergence), write cost within noise — but **lean NO-GO** because it kept the `gph_next_pageno` chain
  (no physical contiguity) so the traversal win was unmeasurable. It surfaced two real findings: **GenericXLog
  caps at 4 buffers/record** (an atomic hub re-sort needs a shadow-extent swap, not a golden-rule-2-forbidden
  custom rmgr) and `gph_neighbors` re-read a page per `Next()`. **Round 2 (the re-run, branch
  `spike/009-contiguity` `d3a6756`) closed both gaps:** a real contiguous-extent allocator (`gph_extend_pages_contig`
  + relayout-on-grow — proven under *interleaved* load: sorted hubs land on consecutive blocks 5046,5047,5048
  while the chain scatters to 5029,5031,5033) and a **read-once-per-page scan** (full deg-5000 hub:
  **5005 → 6 `ReadBuffer` calls, ~830×**). FR-7 still zero-divergence; correctness green; bulk load −1.0%
  (noise). Under tiny `shared_buffers` the win shows in page-count + **−11%/−15% warm-cache wall-clock**.
  **Verdict: STILL-INCONCLUSIVE, leaning GO** — every gate clears except the **disk-seek/readahead magnitude**
  (working-set > RAM), which a warm x86 cache structurally cannot show; that one number needs the GX10's
  NVMe+128 GB regime (or dropped caches / O_DIRECT). **Prototype NOT merged** (HIGH-risk migration; not a hard
  GO) — it lives on `spike/009-contiguity` for the GX10 confirmation; only the design-note verdict landed on
  master. *Shippable-independently:* the read-once scan fix (~830× fewer buffer lookups) is a real win even
  without the migration — a candidate standalone change for the maintainer to weigh.

**Plan 014 (project backlog)** — DONE: the manual engine CI job now runs `make test-all` (one-line change,
YAML valid, host `make test`/`lint` green). The real engine run happens on GitHub. Branch
`advisor/014-ci-test-all` (`22d32b6`); `plans/README.md` row updated to DONE.

**Cherry-pick notes for the maintainer:**
- The 006 and 007 executor branches each also added a row to `advisor-plans/README.md` off a **stale base**
  (the worktree's committed copy, not this authoritative main-tree index). **Skip that one file** when
  cherry-picking — take the code/doc deliverables, not the index edit.
- 007 and 008 both add a `Makefile` target (different targets, adjacent lines) → trivial conflict if both
  land. 006 ↔ 009 don't overlap. Recommended land order: **006 → 007 → 008**, then 009 if the prototype
  is greenlit.
- Branches persist in git; the scratchpad worktrees are disposable. Merging is the maintainer's call.

## Dependency notes

- None hard-block each other. 001 and 005 are independent quick wins. 003 extends the reloptions
  patch shipped in PR #21; 004 codifies the sweep recipe that produced `bench/results/neon_sweep_*`.

## Findings considered and not turned into standalone plans

- **LegStats struct duplicated** (`src/planner/join_order.c` static vs `src/planner/join_order_legstats.h:32`):
  real drift risk but LOW priority and GX10-only; the `.h` already documents that consolidation happens
  when the Option-B integration lands — tracked by Linear **DEV-1290** (filter-first operator). Fold there.
- **Launch-path direction items** (100k/768 headline recall curve with NEON latency; public-dataset
  benchmark + tuned baseline + dataset choice; the inert-until-Stage-3 join-order heuristic; graph
  `avg_out_degree`): already tracked in Linear — **DEV-1286** (sweep/headline), **DEV-1290** (filter-first
  operator, the v1.1 unlock that makes the join-order decision actually execute), **DEV-1289**
  (`gm_edge_count`), and `docs/gtm_opensource_v0.1.0.md` (public dataset + repro). Not duplicated as plans.
- **`live_report.py` parser edge-case coverage**: real but lower leverage than 001; defer until the
  live report consumes real-dataset recall (the seam noted in `docs/STATUS.md` DEV-1284 row).

## Direction summary (for the maintainer, not ranked against bugs)

The architecture (TR-1 / no blocking operators / global top-k) and the decision core are proven;
latency is now real (~1.8 ms on the GX10). The credible-launch gate is **evidence at scale + a stranger
can reproduce it**: the 100k/768 recall curve (DEV-1286) and a pinned public-dataset run with a
one-command repro (the GTM "make-or-break"). The join-order heuristic is shipped but **inert until the
filter-first operator body lands (DEV-1290)** — message that honestly as a v1.1 unlock, not a v1 feature.

---

## Deep-audit batch (2026-07-01, planned against `408e852`)

Generated by the `improve` skill's `deep` variant: 8 parallel category auditors (correctness-C,
correctness-Python, security, performance, tests, tech-debt, deps/DX, docs/direction) over the
whole repo, every finding re-verified against the code by the advisor before planning. ~66 raw
findings → vetted table → these 8 plans (all 8 clusters selected by the maintainer).

| Plan | Title | Priority | Effort | Risk | Depends on | Verify here? | Status |
|------|-------|----------|--------|------|------------|--------------|--------|
| 010 | tjs_open: unify heap metric (squared L2), bounded bridge share, wire orphaned smoke test | P1 | M | MED | — (before 017) | patch-chain CI here; engine run gated | DONE — patch-chain `ci_check_patches.sh` + `make test`(205)/`lint` green; bridge key squared to shared L2 unit, k/2 bounded bridge share in `finalizeTopKO`, smoke test extended (ordering + blend assertions) + wired into `ENGINE_TESTS` (engine run gated) |
| 011 | tjs/tjs_open STRICT + NULL guards; quote table_name (make SECURITY.md true); CI permissions + SHA pins | P1 | S | LOW | — | patch-chain CI here; engine run gated | DONE — patch-chain `ci_check_patches.sh` + `make test`/`lint` green; NULL-arg asserts added (engine run gated) |
| 012 | Wire orphaned engine tests: HNSW recovery oracle, missing reloptions driver, tjs double-scan regression, sleep→poll, dead runner | P1 | M | LOW | — | bash -n / make -n here; engine run gated | DONE — 3 scripts appended to `AM_TESTS` (`crash_recovery_hnsw`, new `crash_recovery_reloptions_test.sh` two-phase seed/assert driver, new `fork_bug_tjs_double_scan_test.sh`); bare `sleep 1` in `crash_recovery_hnsw_test.sh` replaced with bounded fail-loud postmaster-exit + `SELECT 1` polls (same in reloptions driver); `test/fork_bug_tjs_double_scan.sql` passing regression (sibling scan + tjs() in one block) + witness header de-drafted; Step 5 no-op (tjs_test.sh kept). `make test`(205)/`lint`/`bash -n`/`make -n graph-test` green; live engine run of the 3 new suites gated: unbuilt here |
| 013 | Docs/number reconciliation: one SM-2 value, true scale labels, ADR-0011/0012 + STATUS + spec markers current | P1 | S | LOW | — | YES (greps) | DONE — one canonical SM-2 (15.1×); greps + `make test`/`lint` green |
| 014 | Bench grading hardening: DONE-gates everywhere, one baseline-merge semantics, reader-failure tally, one recall_at_k | P2 | M | MED | — | YES (`make test`) | DONE — 5 DONE-gates + tests; live_report ANN-pruned merge unified with sm2 (no golden move: toy corpus < k*32); graphrag reader_failures tally; `bench.metrics.recall_at_k` single home (sweep `_recall` deleted, recall_decay scoring-line repointed); h2h/ablation/rabitq copies LEFT per the empty-oracle divergence guard; `make test`(205)/`lint` green |
| 015 | Toolchain determinism: lockfile, pinned ruff + pyproject, venv-consistent lint, sed post-conditions, .env.example | P2 | S | LOW | — (coord. 011 on ci.yml) | YES | DONE — `requirements.lock` (121 pins via `uv pip freeze`, no editable/local lines) + `make lock` regen; CI installs from lock (rebased onto plan 011's SHA-pinned `ci.yml`); `pyproject.toml` pins ruff `target-version=py312`/`line-length=88` (format clean at 88, no reformat) + `ruff~=0.15.18` floor; `make lint` now `$(PY) -m ruff …` (passes with NO venv on PATH); `harden_dockerfile_downloads` gains fail-loud post-conditions (rebased onto plan 021 edits — verified die-on-drift AND pass on pinned upstream Dockerfile via mutation sim); `.env.example` (gitignore negation, not ignored) + README lock/env/PGPORT notes. `make test`(205)/`lint`/`bash -n`/`make -n graph-test,smoke-test`/`ci_check_patches.sh` all green |
| 016 | v0→v1 graph-store rewire — DESIGN plan (ADR-0013 + measured spike; migration is a follow-on) | P2* | L | MED | — | harness lints here; spike run gated | DONE — ADR-0013 + `docs/graph_rewire_design_v0.1.0.md` (108-hit interface-gap matrix, id-strategy = `gph_upsert_vertex` mapping layer, staged A/B/C plan, 3 riders w/ file:line) + `scripts/graph_v0v1_bench.sh`/`test/graph_v0v1_bench.sql` spike (`bash -n` clean; live run engine-gated) + STATUS ADR-0013 banner; `make test`(205)/`lint` green. Migration itself is a follow-on after ADR acceptance |
| 017 | tjs_open batched BFS (one SPI round trip per hop) + fail-loud SQL-buffer truncation | P2 | M | MED | 010 | patch-chain CI here; engine run gated | DONE — `expandMultiSeedO` now issues ONE `SPI_execute` per hop via `LATERAL unnest(ARRAY[...])` (per-node `neighbors(%lld)` gone, `cmd` off the stack); both `sourceText` snprintf sites guarded with `PROGRAM_LIMIT_EXCEEDED` ereport; smoke test extended with a batched-BFS set-equivalence assert (2 overlapping seeds, 2-path node); `ci_check_patches.sh` + `make test`(205)/`lint` green (engine run gated) |

\* 016 is strategically P1 — the audit's biggest finding is that **both shipped operators and all
benchmark scripts run on the v0 heap-backed `graph_store_ext`, not the v1 native AM** (every
published headline measured the predecessor store). It is sequenced P2 only because the P1
correctness/test/docs plans are cheap and de-risk everything else first.

Recommended order: **011 → 012 → 010 → 013 → 015 → 014 → 017 → 016** (011/012/013/015 are
independent and can interleave; 017 strictly after 010 — same patch file; 016's follow-on
migration plans come only after ADR-0013 is accepted; the 128 GB headline run should wait for
the 016 decision so it measures the right store).

### Key vetted findings NOT turned into plans this round (pick up later, don't re-audit)

- **HNSW in-RAM index cache never invalidated across backends** (rebuild-on-recovery patch;
  `vector_index_map` per-process, no epoch) — stale ANN reads in pooled deployments. LOW
  confidence it's unintended (bulk-load-then-query pattern); needs a documented contract or an
  epoch — investigate alongside DEV-1259 Phase C.
- **Operator mid-drain error path leaks** (`PG_CATCH` in both tjs patches pops the snapshot but
  skips `End*State`/`SPI_finish`) — bounded per-error leak; fold into the next patch-touching
  change.
- **SM-1 peak model makes the live_report ratio ~1× by construction** (`peak=max(k,reached)` vs
  baseline `max(160,…)` at fanout≈150) — investigate which measurement backs the published SM-1
  before DEV-1284.
- **Ablation `_query_entity_seeds` skips single-token entity titles** (`range(2, maxlen+1)`) —
  possible downward bias on the published "graph adds ~nothing on news" negative; re-run with a
  df-guarded length-1 floor and compare.
- **`vec_literal` ×7 and docker-exec boilerplate ×16** — consolidation follow-on to plan 014
  (recall_at_k lands the pattern; do vec_literal next, then `scripts/lib/pg_in_image.sh`).
- **Superseded `hnsw_wal_durability.patch` sits in the active patches dir** (inert draft,
  apply-call commented out) — move to `scripts/patches/archive/` with an ADR-0009 pointer.
- **`gist-960-euclidean` registry entry unpinned (`_PENDING`) + `http://` mirror base** in
  `tools/fetch_dataset.py` — pin on first real fetch via the existing `--pin` flow; switch to https.
- **Compose baseline images by tag not digest; MinIO/etcd 2022-23 era** — baseline-only blast
  radius; bump when the baseline is next touched.
- **`graph_concurrency_test.sh` equal-budget race** (30 s hold vs 30 s cumulative poll) — widen
  the hold; fold into the next test-touching change.
- **Predicate-aware early-termination regression missing** (the DEV-1169 scale-defect class) —
  needs a selective-predicate corpus design; natural rider on DEV-1284's re-measure.
- **Stub bench validates src-ordering; realized operator ranks by dst** — documented spec-vs-
  realized drift; decide when DEV-1284 reopens the SM harnesses.
- **VDBB adapter invents `label = id % 100`, ignores `labels_data`** — verify VectorDBBench's
  NumGE ground-truth contract before the 768D1M1P headline run.
- **MuSiQue/2Wiki eval bar (DIR-03)** — ADR-0012's own addendum says `r_max` is inert on
  HotpotQA; a dense-graph benchmark is the only way to show the termination curve bending.
  Harness-only, M — strong candidate for the next direction plan.
- **BM25-seam design spike (DIR-04)** — architected-but-closed, explicitly deferred to a future
  ADR; post-launch bet, not a launch item.

### Considered and REJECTED this round (do not re-propose)

- **vdbb table-identifier quoting**: `TABLE = "vdbb_tridb"` is a local constant; no untrusted
  path. Not worth doing.
- **PG 13.4 rebase**: locked decision; cost quantified (no upstream security backports; PG 13
  line at EOL horizon Nov 2026) and now stated in SECURITY.md — track as risk, no plan.
- **Pre-commit hooks**: CI already gates lint/format; marginal.
- **float64 recall-decay oracle**: float32 BLAS tie-order jitter is within the report's own
  noise floor; revisit only if a decay claim ever rides a near-tie margin.
- **tjs() metric audit**: verified clean — single-source tjs ranks ONLY on `xs_orderbyvals`
  (one unit); the mixed-metric bug is tjs_open-specific.
- **Shell scripts set -e / exit-code audit**: verified clean — all first-party scripts carry
  `set -euo pipefail` and fail-loud patterns; the vendored `patch.sh` gap is compensated by
  `verify_patches`.

---

## Upstream (vendored MSVBASE) deep-audit batch (2026-07-01, planned against `408e852`)

Generated by the `improve` skill's `deep` variant, scoped to the **vendored upstream** at
`vendor/MSVBASE` (pinned commit `1a548db`) that TriDB inherits — distinct from the 2026-07-01
first-party batch (010-017). 5 parallel auditors over pristine upstream (read via `git show HEAD:`,
excluding TriDB's own patch hunks): HNSW/index scan path, topk/multicol operators, the
Postgres/hnsw submodule patch layer, build/supply-chain, plus an upstream PR/issue assessment.
Every planned finding re-verified against the (pristine and patched) tree by the advisor.

**Upstream PR/repo assessment (the "check on their open PRs" ask):** microsoft/MSVBASE has **zero
open PRs**; the pinned commit `1a548db` is **byte-identical to `main` HEAD** (`ahead 0, behind 0`) —
TriDB pins the latest upstream, nothing to pull. Repo is effectively **frozen** (last code push
2024-11-19; 20 PRs ever; not archived; MIT; 103★). Treat as a permanent hard fork — no upstream
fixes are coming. Live signal is in open issues: **#21** (Boost jfrog build failure) is **already
fixed** by TriDB plan 007; **#23** (slow single-threaded HNSW build) corroborates the NEON finding
(single-threaded by design, no `threads` relopt — UP-CORE-07); **#19** (multicol TopK unpatched) is
inherited but largely moot for TriDB's direct-call path; **#22** (VBase iterative-search
correctness) maps to UP-PATCH-07 / UP-CORE-01, addressed by plan 022.

Since `vendor/MSVBASE` is gitignored + re-cloned, **every fix is a new TriDB fork patch under
`scripts/patches/`** wired into `scripts/lib/msvbase_patches.sh` with a `verify_patches` sentinel
(or a build-script/Dockerfile-harden change). All 6 clusters selected by the maintainer.

| Plan | Title | Priority | Effort | Risk | Findings | Verify here? | Status |
|------|-------|----------|--------|------|----------|--------------|--------|
| 018 | Neutralize the unused PostgresMain query rewriter | P1 | M | MED | UP-PATCH-01..06, 09 | patch-chain CI here; engine gated | DONE — `tridb_remove_pgmain_rewriter.patch` deletes the whole approximate_sum→topk rewrite block from PostgresMain (restores plain `query_string`/`exec_simple_query`, one-line sentinel) + its dependent `pfree(result)`; executor hunks (nodeSort/nodeIndexscan relaxed-mono) LEFT intact for plan 022. Wired LAST in `apply_tridb_fork_patches` (applies inside `thirdparty/Postgres`), 2 `verify_patches` sentinels (sentinel present AND `approximate_sum(` absent). `ci_check_patches.sh` exit 0 (`grep -c approximate_sum( postgres.c` = 0) + `make test`(205)/`lint` green; `test/pgmain_rewriter_removed.sql` wired into ENGINE_TESTS (live engine run gated: unbuilt here) |
| 019 | HNSW AM entry guards (dim/array validation, endscan NULL, BulkDelete, range dist) | P1 | M | LOW | UPCORE-01/03/07, UP-CORE-03/10 | patch-chain CI here; engine gated | DONE — `tridb_hnsw_am_entry_guards.patch` (LAST in chain) + 2 sentinels; `ci_check_patches.sh` + `make test`(205)/`lint` green. 4 additive guards: gettuple dim check (ORDER BY `== dim`, range `== dim+1`/non-empty), `convert_array_to_vector` ARR_HASNULL/FLOAT8OID/NDIM, BulkDelete NULL-stats palloc0 + `ItemPointerSet`, range-dist `lhs == rhs-1`. Step-3 endscan NULL guard was ALREADY present (DEV-1236 scan_no_orderby) — not duplicated. `test/hnsw_am_guards.sql` wired into ENGINE_TESTS (live engine run gated: unbuilt here) |
| 020 | Fix NULL-rank-as-nearest in tjs/tjs_open (castAttrToFloatT) | P2 | S | LOW | UPOPS-01 | patch-chain CI here; engine gated | REJECTED — verified 2026-07-02: `castAttrToFloatT` has ZERO call sites in `tridb_tjs_operator.patch`; `execTJS` ranks solely on `xs_orderbyvals[0]` and `tjs_open` already maps NULL bridge distance to +inf. The upstream `castAttrToFloat` NULL→0 bug is real in `topk.cpp` but the TriDB fork never wires the helper into the queue key, so the bug cannot fire on either operator. Non-finding for TriDB. |
| 021 | Upstream build/image hardening residuals (git TLS, base digest, SPTAG fetch, superuser) | P2 | S | LOW | UP-BUILD-01/02/04/05 | bash -n / ci_check here; build gated | DONE — `harden_dockerfile_downloads` now deletes `RUN git config --global http.sslverify false` (UP-BUILD-01) and `patch_upstream_dockerfile` digest-pins `FROM gcc:12.3.0@sha256:de5c…1690e` (UP-BUILD-02, digest resolved via `docker buildx imagetools inspect`, recorded as `GCC_12_3_0_DIGEST` with dated comment); both build scripts scope submodule init to `thirdparty/Postgres thirdparty/hnsw` + `GIT_LFS_SKIP_SMUDGE=1` (UP-BUILD-05); SECURITY.md gains the entrypoint superuser/`0.0.0.0/0` publish-time note (UP-BUILD-04). Mutation sim over pristine Dockerfile: `sslverify false`→0, FROM digest-pinned, idempotent; `bash -n` + `ci_check_patches.sh` exit 0; `make test`(205)/`lint` green. `scripts/x86build.sh --docker` engine-gated: unbuilt here. UP-BUILD-03 (apt pinning) accepted gap per scope. |
| 022 | Relaxed-monotonicity executor hardening (xs_inorder, wrong-order guard, window constants; issue #22) | P2 | M | MED | UP-PATCH-07, UP-CORE-01/02 | patch-chain CI here; engine gated | DONE — `tridb_relaxed_order_executor_guard.patch` (LAST in chain, applied from `$root` since it spans the Postgres submodule AND `src/hnswindex.cpp`): genam.c zero-inits `scan->xs_inorder`; nodeIndexscan.c gates `is_index_inorder` on `scandesc->indexRelation->rd_indam->amcanrelaxedorderbyop` (both in-order + reorder paths) and restores the `index returned tuples in wrong order` ERROR for NON-relaxed AMs; window constants (86/3/50) DOCUMENTED not parameterized (plan Step-3 option (b) — code comment + ADR-0007 addendum, issue #22 cited). 2 load-bearing sentinels (genam xs_inorder zero-init + nodeIndexscan amcanrelaxedorderbyop). `ci_check_patches.sh` exit 0 + `make test`(205)/`lint` green; `test/relaxed_order_guard.sql` wired into ENGINE_TESTS (live engine run + recall/latency sweep gated: unbuilt here) |
| 023 | HNSW index-map cache invalidation — design + spike (DROP/REINDEX stale reads) | P2 | M | MED | UPCORE-02, UP-CORE-12 | harness lints here; spike gated | DONE — `docs/decisions/0014-hnsw-index-cache-invalidation.md` (code-verified cache facts at `408e852`: no `CacheRegisterRelcacheCallback`/`.erase`/`relfilenode` exists → STOP-1 not a false alarm; static map ⇒ one backend suffices → STOP-2 clear; two-option analysis, recommends Option A relcache-callback eviction re-keyed on `relid` with the shared_ptr ownership/refcount rule) + repro `scripts/hnsw_stale_index_repro.sh` + `test/hnsw_stale_index.sql` (four scenarios A–D on ONE persistent backend: DROP+CREATE same name / REINDEX / dim-change OOB; `bash -n` clean, live run engine-gated: unbuilt here) + STATUS ADR-0014 banner. Production fix deferred to DEV-1259 Phase C per scope. `make test`/`lint` green |

Recommended order: **019 → 020 → 018 → 021 → 022 → 023**. 019 (crash/OOB on the hot path) and 020
(ranking bug, edits TriDB's own patches) are the cheapest highest-value; 018 removes a whole class
at once but is a bigger hunk; 021 is independent build work; 022 touches the thesis-critical
executor (do after the cheap crash fixes, with recall validation); 023 is a design/spike feeding
DEV-1259. **Note 018 vs 022**: both edit `Postgres.patch`'s output — 018 removes the *string-
rewriter* hunk, 022 hardens the *executor* hunk; keep them as separate patches, coordinate if the
hunks are entangled.

### Upstream findings NOT turned into plans (verified; pick up later)

- **UP-CORE-07 / issue #23** — HNSW index build is single-threaded (no `threads` relopt, serial
  `addPoint`). Real perf ceiling TriDB inherits; L effort (parallel build correctness). The NEON
  patch already cut per-distance cost 4-8×; a parallel build is the next lever for the 128 GB run.
- **UP-CORE-05** — `execFagins` termination is a heuristic drop-count, not a sound TA/NRA bound;
  approximate top-k whose quality is a hidden function of `term_cond`. Matches the TriDB TJS
  termination memory (term_cond is the recall knob; report SM-4 as a curve). Document at the
  algorithm boundary; a true per-stream lower-bound stop is a research-grade follow-on.
- **UP-CORE-12** — unbounded `ORDER BY v <-> q` without LIMIT degrades to O(N) full-graph traversal;
  fold an ef/k traversal budget into the operator (partly overlaps 023's cache note).
- **UP-PATCH-08** — hnsw `ResultIterator` `candidate_set` is never pruned to `ef`, growing toward
  O(edges-visited) across a long relaxed scan (memory, not correctness); cap/prune it. Also the
  `cand < 0` dead check / `> vs >=` off-by-one (UP-CORE-11) — trivial, fold into 019 if convenient.
- **UPCORE-04 / UP-OPS-03** — SQL injection via unquoted `table_name` in upstream `model_mng.cpp`
  `inference()` and the `getrelid` helper. LOW TriDB exposure (`inference()` off the hot path;
  `getrelid` table_name is covered for tjs/tjs_open by first-party plan 011). Note for completeness.
- **UP-CORE-08** — `multicol_topk.cpp` is a near-verbatim drift-prone fork of `topk.cpp`; every
  topk fix must be mirrored. Consolidation is a tech-debt follow-on (and moot once plan 018 removes
  the only implicit multicol route).
- **UP-BUILD-03** — unpinned `apt-get install` in the Dockerfile (reproducibility-only; apt verifies
  repo signatures). Accepted gap; pin if/when the toolchain is frozen for release.

### Considered and confirmed CLEAN / mitigated (do not re-audit)

- Plan-007 Boost/CMake download hardening — **verified complete** (TLS restored, sha256 enforced,
  dead jfrog URL rewritten; the only two Dockerfile downloads).
- ARM x86-ISA-flag strip — **complete** for the two CMake files it processes (+ post-condition).
- `verify_patches` — catches a silent `patch.sh` failure for all three base submodule patches
  (fail-loud), so upstream `patch.sh`'s missing `set -e` is compensated.
- pgvector-#862 pathkey landmine — **avoided**: `create_ordered_paths` forces `is_sorted=false` for
  relaxed indexes, so the planner keeps an explicit Sort (no skipped-sort wrong results).
- `castToUint64` tid packing (topk/multicol) — **collision-free** (disjoint 16-bit slots in 48 bits);
  the separate `ItemPointerDataHash` signed-shift UB is on a **dead** functor (seenSet is
  `unordered_set<uint64_t>`), cosmetic only.
- `EndFaginsState` UAF/mismatched-free — **already compensated** by TriDB's snapshot/UAF patch (the
  upstream defect is real but TriDB fixed it; other forks inherit it).
- No committed secrets, no prompt-injection/instruction-bearing content in any audited upstream file.

---

## 2026-07-15 batch — post-D1/D2 deep audit (planned against `a41b0c7`)

Deep `/improve` audit (6 parallel category auditors + advisor re-vet against source) after the D1
demonstrated-architecture pass (Gate A, tag `v0.1.0`) and the D2 un-fork (Gate B PASS on stock
PG17+pgvector, `src/tjs_pg` operator, ADR-0019). Findings target this session's **new** surfaces:
the stock-PG C operator, the Wikidata benchmark pipeline, the fork↔stock duality, and the outward
docs. Every planned finding was re-opened against live code at `a41b0c7` and verified by the advisor.

Python layer verifies here (`make test`=pytest, `make lint`=ruff). Stock-PG engine builds off-GX10:
`docker build -t tridb/pg17-unfork:dev scripts/pg17/` then
`bash scripts/pg17_graph_test.sh <image> test/<suite>.sql` (PG16 via `--build-arg PG_MAJOR=16`).

| Plan | Title | Priority | Effort | Risk | Verify | Status |
|------|-------|----------|--------|------|--------|--------|
| 062 | `tjs_open` NULL-arg guards (stop backend segfault; regression vs fork STRICT) | P1 | S | LOW | stock-PG engine | **DONE** (merged `59539a4`; advisor-reviewed APPROVE: PASS 3b + ALL PASS PG16+PG17, exit 0, clean scope) |
| 063 | `tjs_open` filter-first ranks by the index's actual metric (not hardcoded L2 `<->`) | P1 | S | LOW | stock-PG engine | **DONE** (merged; advisor-reviewed APPROVE: PASS 1c cosine-discriminating test + ALL PASS PG16+PG17, neg-control confirms fail-pre-fix; 2 justified deviations — syscache OPERATOR render + real discriminating test) |
| 064 | Wikidata baseline recall reproducibility (deterministic Neo4j subset + oracle/engine tie-break) | P1 | S | LOW | YES (python) | TODO |
| 065 | Orphaned gate-env var (`WD_`/`WH_` drift) + report zero-guard + numeric-id item check | P1 | S | LOW | YES (python) | TODO |
| 066 | Pin the pgvector base image + assert pgvector ≥0.8 at CREATE EXTENSION | P1 | S | LOW | stock-PG engine | TODO |
| 067 | Reconcile outward docs with D2 (README/STATUS/CLAUDE/CONTRIBUTING/ADR-0015/INSTALL) | P1 | S | LOW | docs-only (grep) | TODO |
| 068 | Public wiki_reader hardening (LLM-route DoS bound, error-leak, response headers) | P1 | S–M | LOW–MED | manual (live host) | TODO |

**Recommended order**: 062 → 063 (both touch `tjs_pg.c`; land the segfault guard first, keep diffs
clean), then 064, 065, 066 (independent, any order), 067 + 068 (docs / service, independent). No
plan depends on another; 062 before 063 is a diff-hygiene preference, not a hard dependency.

### Considered — verified real, not yet planned (available on request; lower leverage or larger)

- **[C] `budget_capped` over-reports** (`tjs_pg.c:534-543` flags any stream end without term_cond,
  incl. natural index exhaustion / `term_cond==0`) — measurement-integrity, S. **[C] filter-first
  `tjs_examined` reports post-LIMIT survivor count, not work** (`tjs_pg.c:365`) — skews cross-path
  work comparison, S. Bundle into a "tjs_pg counter honesty" plan.
- **[PERF] per-candidate SPI in the seedless path** — the dominant, self-admitted MVP cost
  (`tjs_pg.c:479-486` re-fetches a row already in `slot`; `:577-607` one SPI per bridge; `:242-267`
  one SPI per seed). M/MED-risk (touches operator core) — a real optimization plan, worth doing when
  seedless latency matters (filter-first, the headline, is unaffected).
- **[PERF] engine loader does two full disk passes over edge shards** (`wikidata_engine_load.py:404,427`),
  M. **[PERF] oracle computes each query's candidate set twice** (`wikidata_h2h.py:722-726`), S.
  **[PERF] `wikidata_ingest` pure path re-scans the dump per hop with no guard/warning** (S).
- **[TECH-DEBT] two divergent `tjs_open` implementations (fork C++ vs stock C) with parity in prose
  only** — add a differential golden-output test (M, test-only). **[TECH-DEBT] slice-loading logic
  duplicated between `wikidata_engine_load` and `wikidata_h2h`** (extract a shared `wikidata_slice`
  module, M). **[TECH-DEBT] string-typed `dialect` threaded through 6+ sites, silent fork default**
  (introduce a dialect descriptor, M). **[TECH-DEBT] two plan dirs numbered independently**
  (`plans/` 001-015 vs `advisor-plans/` 001-068) — namespace note, S.
- **[TESTS] stock-dialect engine-loader branch untested** (`test_wikidata_engine_load.py` only
  exercises `dialect="fork"`), S. **[TESTS] `bench/wikidata_sm4_seedless.py` has zero host tests**
  (extract the recall reducer into a testable seam), S. **[DX] stock-PG build/test path absent from
  CLAUDE.md + no Makefile target** (add `make stock-graph-test`), S. **[DX] new harness env vars
  undocumented** (`WD_ENGINE_DIALECT`/`WD_SLICE`/`WD_EMB`/`WH_*`) — env table in INSTALL doc, S.
  (DX-01/02 partially covered by plan 067; the Makefile target is the residual.)
- **[SECURITY] wiki_reader overlay-mutation trust** — `accept_fact` (`wiki_reader.py:1985-2015`)
  persists client fields without re-running the grounding check, gated only by a page-embedded token
  (content-spoofing on the public wiki), M/MED. Separate from plan 068 (needs an operator secret not
  rendered into HTML + server-side re-validation). **[SECURITY] baseline compose binds datastores on
  `0.0.0.0` with weak default creds** (`baseline/docker-compose.yml`) — prefix `127.0.0.1:`, S.
  **[SECURITY] `tjs_open` interpolates raw `filter` text into SPI SQL** (`tjs_pg.c:357,432,571`) —
  mitigated by `REVOKE EXECUTE`; document the trusted-caller contract, defense-in-depth only.

### Considered and rejected (do not re-audit)

- **tjs_pg resource/lock leaks on error paths** — verified NOT a leak: transaction/subtransaction
  abort releases the index scan, locks, buffer pins, and SPI connection; exactly one `SPI_finish`
  per path; no double-finish. (The real backend risk is the NULL-arg segfault — plan 062 — which
  *bypasses* this cleanup.)
- **tjs_pg top-k heap / snapshot / SPI-nesting correctness** — verified correct (bounded max-heap,
  finalize dedups within k, `gph_traverse_bfs` is pure-C so no nested `SPI_connect`).
- **un-fork PG14/15 version guards + BLCKSZ≥8192 geometry** — verified correct on both 8KB and 32KB.
- **dense-id contract drift (embed/loaders/harness)** — verified byte-identical first-wins across all
  four; not a finding. **orjson pinned** (`requirements.lock:31`), not drift.
- **wikidata SQL/Cypher f-string building** — values come from the controlled slice + operator CLI,
  not network input; safe as local benchmark tooling (flag only if ever fed untrusted input).

### Direction (options for the maintainer — grounded, not ranked against bugs)

Design/spike-shaped; each cites in-repo evidence. Not written as plans yet — pick to spec.

1. **Lift the seedless recall ceiling on stock PG** — SM-4 measured a ~0.83–0.85 recall@10 plateau
   (`docs/sm4_seedless_stock_v0.1.0.md`); two levers are already validated in-repo (advisor plan 008
   4-bit RaBitQ rerank → recall 1.0 at 7.5× compression; plan 007 FR-fusion → 0.987). Spike: wire one
   into `src/tjs_pg` and re-measure. M.
2. **Native executor for PG19 `GRAPH_TABLE`** — the landscape review (F3) + ADR-0015 flag that PG19
   lowers `GRAPH_TABLE` to relational joins; the un-fork makes "serve the stock parser natively" the
   adjacent possible. Design ADR mapping the canonical query to the PG19 grammar. L, longer bet.
3. **GraphRAG / agent-memory adapter + MCP server on the stock image** — landscape §2 #4/#8 + the
   roadmap's named "proven fits"; a gBrain adapter prototype already exists. D2's installable image
   makes this the highest-leverage post-benchmark credibility artifact. M–L (cross-repo).
4. **"TriBench" — the reproducible cross-modal benchmark the field lacks** — the Wikidata spike +
   `publication_gate` are already the seed; generalize into an externally-citable standard. L,
   positioning.
5. **Certified rank-join bound for `term_cond`** — landscape §2 #5 (HRJN/Tziavelis); turns SM-4's
   recall *curve* into a guarantee-vs-latency curve and answers the DEV-1169 defect class
   structurally. L, research-grade, least certain.
6. **Complete the seedless SM-4 parity vs the fork** — bridge *mechanism* parity already landed
   (`81b8023`); the remaining gap is measuring the stock seedless SM-4 curve against the fork's, so
   the un-fork is a proven strict superset. M, sequences naturally after the above.
